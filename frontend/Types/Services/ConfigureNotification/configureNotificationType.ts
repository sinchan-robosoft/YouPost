
export type configureNotificationType = {
    shouldShowAlert?: boolean,
    shouldPlaySound?: boolean,
    shouldSetBadge?: boolean,
    shouldShowBanner?: boolean,  
    shouldShowList?: boolean,    
}